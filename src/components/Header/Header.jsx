import React from 'react';
import "./Header.scss";
import Nav from "./../Nav/Nav";

export default function Header(props) {
    return (
        <header>
            <Nav />
        </header>
    )
}